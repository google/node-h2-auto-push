{
  "name": "h2-auto-push",
  "version": "0.1.0",
  "description": "HTTP/2 auto-push library",
  "main": "build/src/index.js",
  "types": "build/src/index",
  "scripts": {
    "test": "ava build/test/*-test.js",
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc -p .",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run check"
  },
  "keywords": [
    "http2",
    "auto-push",
    "server-push"
  ],
  "author": "Google LLC",
  "license": "Apache-2.0",
  "repository": "github:google/h2-auto-push",
  "dependencies": {
    "bloomfilter": "0.0.16"
  },
  "devDependencies": {
    "@types/node": "^8.5.2",
    "ava": "^0.24.0",
    "gts": "^0.5.2",
    "typescript": "~2.6.2"
  }
}
